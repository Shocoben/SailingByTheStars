void InvertProcessFragShader(float2 uv : TEXCOORD0, out float4 color : COLOR, uniform sampler2D texture)
{
	float4 temp_color = tex2D(texture, uv);

	color = float4(1 - temp_color.r, 1 - temp_color.g, 1 - temp_color.b, 1);

}